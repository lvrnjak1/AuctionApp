import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#8367D8"
        },
        secondary: {
            main: "#252525"
        }
    },
    typography: {
        fontFamily: "Lato"
    }
});

export default theme;